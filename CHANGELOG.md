# 更新日志

## [1.0.10] - 2026-09-11

### 新增

- 员工守则支持从 Dify 同步，并在桌宠气泡中展示分类提醒。
- 新增工作日随机小贴士，支持手动抽取、继续阅读上一条和查看最近小贴士。

### 优化

- 小贴士按内容长度自动延长展示时间，鼠标悬停时暂停倒计时。
- 改进桌宠在多显示器、边缘拖动和气泡布局变化时的位置稳定性。
- 更新下载时在设置页显示可视化进度条、百分比、已下载大小和实时速度。
- 更新检查、等待确认、下载、安装和失败状态会自动切换进度条显示，安装阶段显示自动重启提示。

## [1.0.9] - 2026-09-08

### 修复

- 修复显示器布局变化或断开副屏后，控制面板沿用旧坐标并打开在屏幕外的问题。
- 打开控制面板时会校验当前所有显示器；离屏窗口自动移回主屏并保存新位置。
- 已经打开的控制面板也会在再次点击入口或显示器热插拔后自动恢复到可见区域。

## [1.0.8] - 2026-09-08

### 优化

- 已安装 FORCOME AI PWA 仅在检测到对应 `--app-id` / `--app` 启动标记时复用，避免误打开普通浏览器网页。
- 已有钉钉授权凭据时，控制面板登录入口会直接自动启动 CLI 连接器；首次登录仍按服务端要求在浏览器完成一次钉钉授权，托盘菜单保留强制重新登录 / 切换账号。
- 新增可选的美国联邦节假日同步，官方来源为美国人事管理局（OPM），支持每日缓存和离线回退。
- 中国与美国节假日设置合并为一个设置卡片，保留两个独立开关；日历标签使用 🇨🇳 / 🇺🇸 区分来源。
- 更新下载完成后先停止内置 CLI 连接器并退出主程序，再启动安装器；安装完成后自动重启应用。
- 日期计算支持选择具体时钟时间，并将时间差精确显示到小时和分钟。
- 点击“安装”进入进度页后，安装器会按当前安装目录清理残留的 Electron、FORCOME CLI 与托盘子进程，避免双击安装包时长时间等待，也避免更新安装再次弹出“无法关闭，需要手动介入”。
- 安装时自动迁移并清理旧版独立 FORCOME AI CLI：移除 C 盘默认安装、旧计划任务/自启项/PATH/快捷方式/卸载项及 npm 全局版，只保留当前应用内嵌 CLI；钉钉登录凭据继续沿用。
- 安装器现在会先显示安装进度界面，再在点击“安装”后执行退出程序、旧 CLI 清理和必要的权限提升，避免双击安装包后长时间无界面等待。

### 暂不加入

- 侧边隐藏未作为正式功能并入：当前环境无法进行可靠的桌面窗口实测，避免影响托盘、提醒和 CLI 稳定性。

## 未发布

后续改动记录从这里继续追加。

## [1.0.7] - 2026-09-07

### 优化

- 更新检查发现新版本后先征得用户确认，不再未经同意直接下载。
- 保留 NSIS 差分更新与 blockmap 缓存，减少后续版本升级的下载量。
- 更新下载过程显示进度和速度，方便判断网络状态。
- 更新安装完成后自动退出、安装并重启程序，无需手动重新打开。

### 修复

- 修复更新状态在检查、下载、安装和失败重试之间显示不准确的问题。

## [1.0.6] - 2026-09-06

`1.0.6` 汇总了自 `1.0.1` 以来的 CLI 集成和桌宠体验升级。

### 新增

- 内置 FORCOME AI CLI `0.0.47`，安装一次即可使用 CLI 与康康熊桌宠。
- 单一 FORCOME 状态托盘，显示登录、连接、离线重连和旧连接器冲突状态。
- CLI 连接器异常退出、网络恢复、系统恢复或解锁后的自动重连。
- 免打扰模式：仅保留日程提醒气泡、系统通知以及双击打开 FORCOME AI。
- 中国政府网法定节假日同步，区分红色休假和蓝色调休补班，并支持离线缓存。
- 日历年、月、周、日视图和连续月份滚动。
- 提醒批量管理、导入模板、自然语言导入、强提醒与提前提醒。
- 便利贴任务清单、子任务、优先级和每日任务重置。

### 优化

- 熊本体右键菜单与系统托盘彻底分工；桌宠菜单改为精简的一级快捷入口。
- 便利贴标题移入正文编辑区，标题加粗并与正文用细线分隔；顶部大面积区域用于拖动。
- 桌宠默认采用低资源策略，减少视频预加载和闲置动作。
- 锁屏、休眠和隐藏时暂停或回收桌宠媒体资源，不影响 CLI。
- 安装器、应用窗口和托盘统一使用 FORCOME 图标。
- 设置页统一选项说明，开机启动仅在控制面板配置。

### 修复

- 修复隐藏桌宠并销毁最后一个窗口时连带退出 CLI 的问题。
- 修复托盘状态图标在部分 Windows 环境中消失的问题。
- 修复互动菜单配置变更后没有及时刷新的问题。
- 修复已安装 FORCOME AI PWA 的发现、复用和窗口激活逻辑。
- 修复日历视图切换、连续滚动和拖动日程时的位置跳动。

## [1.0.1] - 2026-09-04

- 修复已安装 PWA 的发现和最小化窗口唤起。
- 增加窗口激活重试和默认浏览器兜底。

## [1.0.0] - 2026-09-02

- 首个公开版本。
