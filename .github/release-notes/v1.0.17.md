## 康康熊桌宠 × FORCOME AI v1.0.17

本版本修复内置 CLI 运行时被部分安全软件误报拦截的问题。

### 修复

- 更新内置 FORCOME AI CLI 的 Node 运行时至官方新版本，降低旧版运行时被安全软件拦截的概率。
- 发布构建增加运行时校验，已知被拦截的旧版运行时不会再次进入安装包。

### 安装说明

- 可直接覆盖旧版本安装。
- 应用数据、日程、便利贴、设置和钉钉授权凭据默认保留。
- 安装包未进行代码签名，Windows 可能仍显示 SmartScreen 提示。
