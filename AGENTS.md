# KangKangPet 项目协作规则

## 适用范围

- 本文件适用于 `D:\项目\KangKangPet` 及其子目录。
- 本项目是 Electron 桌面应用，FORCOME AI CLI、桌宠、提醒管理、日历和更新器都是核心功能。
- 优先保护已有行为和用户数据。任何优化都必须先阅读现有实现、测试和打包逻辑，再进行最小范围修改。

## 工作区和文件保护

1. 开始工作时先检查当前分支、工作区状态、版本号、远端、现有 `dist` 和最近提交。
2. 不使用 `git reset --hard`、`git checkout --` 或其他破坏性回退，不覆盖用户已有改动。
3. 不在项目根目录随意创建临时文件、截图、测试输出或第二份工程。临时调查文件放到系统临时目录或明确的调查目录。
4. `dist` 只保留一份当前免安装目录和当前版本根目录安装包；旧安装包、blockmap 和相关历史文件必须归档到 `dist\setups\<版本>`，不能删除。
5. 不改变原有安装位置、升级器、快捷方式、卸载逻辑和 CLI 目录结构，除非用户明确要求。

## 实现原则

- 优先复用现有模块、IPC、ReminderScheduler、配置存储和渲染器模式，不为局部问题重写核心架构。
- 提醒管理是核心组件：不能因为导入一条异常数据阻断整批有效数据，不能静默丢失日程，保存失败必须回滚并明确提示。
- 支持文本、TXT、CSV、XLSX、JSON、图片、音频和视频等提醒来源时，保持预览、选择、去重、待排期和来源信息逻辑完整。
- 页面交互要以实际工作流为准：拖入和粘贴入口应集中在提醒管理页面，不重复创建会遮挡内容的入口区域。
- 不访问、记录或打包 API Key、密码、令牌和其他秘密。
- 手工编辑使用 `apply_patch`；不得使用 `cat`、重定向或 Python 写文件替代补丁编辑。

## 默认完成流程

除非用户明确要求只分析或暂停，否则一次任务要完成以下闭环：

1. 读取相关源码、测试、配置和历史实现，确认影响范围。
2. 实现修改，并补充与风险匹配的回归测试。大批量、持久化、更新器和安装器改动必须覆盖失败路径。
3. 运行：
   - `npm run check`
   - `npm test`
   - `npm run smoke`
   - `npm run verify:cli-runtime`
   - `git diff --check`
4. 完成一版后默认运行 `npm run dist` 生成当前版本 Windows x64 安装包；需要免安装版时运行 `npx electron-builder --dir --publish never`，刷新 `dist\kangkangpet-portable-<当前版本>`，再运行 `npm run clean:dist`。
5. 核对安装包、blockmap、`latest.yml`、免安装目录、历史归档和 SHA-256。构建成功不等于真实 UI 验收，必须区分自动验证、实际启动和人工视觉/交互验收。
6. 本地实现、测试和打包完成后必须创建 Git 提交，不能因为暂不发布 GitHub 而跳过提交。提交前确认只包含本次改动，使用：
   - 作者：`Linus359 <301956110+Linus359@users.noreply.github.com>`
   - 提交信息：使用清晰的 `fix: ...`、`feat: ...` 或 `chore: ...`
7. 最终报告必须给出：改动摘要、测试结果、提交号、安装包路径、免安装目录、哈希、历史包处理结果，以及是否推送或发布。

## Git 和 GitHub 边界

- 本地 Git 提交是默认流程，必须执行。
- `git push`、创建 tag、创建或修改 GitHub Release、上传安装包属于外部发布，只有用户明确说“上传”“发布”“GitHub Release”“推送 release”等才执行。
- 用户只说“打包”“给我看看”“本地测试”时，完成本地提交和本地包，但不得推送、打 tag 或创建 Release。
- 发布前必须再次核对版本号、提交、作者、tag、安装包、blockmap、`latest.yml` 和 Release 资产；发布失败时停在失败阶段，不能声称已发布。
- 公共更新说明只写用户可感知的变化、安装、兼容性和已知限制，不写内部测试命令、测试数量、CI 状态或调查过程。

## 交互和汇报要求

- 已在本文件或 release skill 中明确的流程，不要再次向用户询问；直接执行并报告结果。
- 发现用户已有改动、旧包或正在运行的进程时，先识别归属和影响，不擅自清理。
- 任何失败都要报告具体阶段、实际错误和已保留的数据；不能用“应该可以”代替验证。
- 运行较久时简短说明当前阶段；完成后只报告高信号结果，不重复无关过程。
